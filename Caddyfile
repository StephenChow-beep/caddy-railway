:{$PORT} {
    # 核心：強迫 Caddy 將外網流量精準轉發給同一個項目內部的 agentmemory 容器的 3113 端口
    reverse_proxy http://agentmemory:3113 {
        header_up Host {upstream_hostport}
        header_up X-Real-IP {remote}
    }
}


